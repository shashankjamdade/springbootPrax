syntax = "proto3";
package playground;
option java_package = "com.playground.grpc.chat";
option java_multiple_files = true;

message ChatMessage { string from = 1; string text = 2; }
service ChatService {
  rpc Chat(stream ChatMessage) returns (stream ChatMessage);
}
